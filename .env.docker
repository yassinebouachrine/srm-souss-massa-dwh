# ============================================================
# SRM Souss-Massa - Configuration DOCKER
# ============================================================

# ─── Environnement ─────────────────────────────────────────
ENVIRONMENT=docker
TIMEZONE=Africa/Casablanca

# ─── PostgreSQL DWH (Dans Docker) ──────────────────────────
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=srm_datawarehouse
POSTGRES_USER=srm_admin
POSTGRES_PASSWORD=srm_secure_password

# ─── ETL Paths (Dans Docker) ───────────────────────────────
ETL_DATA_ROOT=/opt/airflow/data
ETL_RAW_PATH=/opt/airflow/data/raw
ETL_BRONZE_PATH=/opt/airflow/data/bronze
ETL_SILVER_PATH=/opt/airflow/data/silver
ETL_REJECTS_PATH=/opt/airflow/data/rejects
ETL_LOGS_PATH=/opt/airflow/data/logs
ETL_LOG_LEVEL=INFO

# ─── PCWIN (SQL Server distant) ────────────────────────────
PCWIN_HOST=192.168.1.66
PCWIN_PORT=51433
PCWIN_DATABASE=ScadaNetDb
PCWIN_USER=ton_user
PCWIN_PASSWORD=ton_mot_de_passe
PCWIN_DRIVER=pymssql
PCWIN_DATE_MIN=2026-01-01
PCWIN_CHUNK_SIZE=100000

# ─── Airflow System Variables ──────────────────────────────
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
AIRFLOW__CORE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
AIRFLOW__CORE__LOAD_EXAMPLES=False
AIRFLOW__CORE__DEFAULT_TIMEZONE=Africa/Casablanca
AIRFLOW_UID=50000





# ─── CHEMINS DES SOURCES BRUTES (Fichiers d'entrée) ───────────
PATH_RAW_PMAC=data/raw/pmac
PATH_RAW_GSTCOM=data/raw/gstcom
PATH_RAW_INDICATEURS_RECLAMATIONS=data/raw/indicateurs_reclamations
PATH_RAW_RENDEMENTS=data/raw/rendements
PATH_RAW_REFERENTIELS=data/raw/referentiels